# FR-20 System-Scope Availability Evidence

Requirement: FR-20 - When md2pdf is installed as a system-scope installation,
md2pdf shall be invocable by each user account on the host.

Status: `blocked`

Simulation status: `pass`

This evidence file was created during P0 and completed during Stream A strict
finalization. The executable-result fields below are filled from the documented
simulation run; deviations from a real elevated multi-account installation are
called out explicitly.

Post-audit Phase 0 reset on 2026-06-12: this file is preserved as historical
Stream A strict simulation evidence only. It does not prove the global FR-20
release requirement because no real elevated system-scope installation and no
secondary user account were tested.

Post-audit Phase 9 refresh on 2026-06-15: the current Phase 8-10 tarball was
replayed locally on Windows through `npm.cmd run release:verify`. That replay
proves temporary user-scope install, installed Windows `.cmd` shim, installed
`md2pdf --help`, and idempotent reinstall for the current account only. It is
explicitly not a real system-scope multi-account FR-20 proof.

## Evidence Metadata

| Field | Value |
| --- | --- |
| md2pdf version tested | `0.1.2` |
| Commit SHA | `b58c45775b5e25926d7567a230034576949bd603` |
| npm tarball or package source | `md2pdf-0.1.2.tgz` generated by final Point 5 `npm.cmd pack --json`; shasum `cc11a64ec297c708b2178727bd372f753fabee33`; integrity `sha512-KUOkmzNX9/0yaqlkpGBFWwu/WqoWHizE4Fe1xG43cuf8JQfnGmBFaA+s3uOvQRIr3cQraFXhNlqJdO9Kk6bGdw==` |
| Date | `2026-06-11` |
| Author | `Codex` |
| Status | `blocked` for global FR-20 release evidence; simulation mechanics passed |

## Current Phase 9 Refresh - 2026-06-15

| Field | Value |
| --- | --- |
| md2pdf version tested | `0.1.2` |
| Commit SHA | Base commit `72c69ba` plus current worktree changes for Windows package smoke, real-browser cache alignment, release evidence, rebuilt tarball |
| npm tarball or package source | `md2pdf-0.1.2.tgz`; shasum `970226a520e446e6e137d678392ff2da70448ab4`; integrity `sha512-7Tlo4xQqDFocfFQMeQageLU6L+oCOlK70VdU8ldGrXWy/lhe/UHvcywR7CwT0VlS4lKeXMt1in5JDbbsK0YuOg==` |
| Date | `2026-06-15` |
| Author | `Codex` |
| OS and exact version | Microsoft Windows 11 Famille 25H2, version `10.0.26200.8655` |
| CPU architecture | `x64` |
| Node.js version | `v24.16.0` |
| npm version | `11.13.0` |
| Primary/installing account | `gabriel-pc\codexsandboxoffline` |
| Secondary account tested | `n/a` |
| Elevated privilege mechanism | `n/a`; no host-wide administrator install was performed |
| Installation command used | `npm.cmd run release:verify`, specifically `scripts/checkPackage.mjs` running `npm install --global --prefix <temporary-prefix> --cache .tmp\npm-cache <repo>\md2pdf-0.1.2.tgz --no-audit --no-fund --fetch-timeout=30000 --fetch-retries=2` |
| Installed binary path invoked | Windows shim `<temporary-prefix>\md2pdf.cmd`; script verified the installed package CLI exists at `<temporary-prefix>\node_modules\md2pdf\dist\cli.js` |
| Command under test | `<temporary-prefix>\md2pdf.cmd --help` |
| Expected result | Temporary-prefix command is invocable by the current account, exits `0`, prints CLI help, and remains valid after reinstall |
| Observed result | PASS for temporary user-scope simulation: install added 123 packages, reinstall changed 123 packages, installed Windows `.cmd` help output matched expected text |
| Does this satisfy global FR-20 release evidence? | `no` |
| Status | `blocked` for global FR-20 release evidence; current-account user-scope simulation mechanics passed |

The Phase 9 decision is therefore explicit: this repository records a current
simulation, but it is non-equivalent to FR-20 final evidence. A real release
candidate still needs an elevated system-scope install and invocation from a
secondary active user account, or an owner-approved decision that such proof is
not required for the release. No such approval is recorded here.

## Environment

| Field | Value |
| --- | --- |
| OS and exact version | Microsoft Windows 11 Famille 25H2, version `10.0.26200` |
| CPU architecture | 64 bits |
| Node.js version | `v24.16.0` |
| npm version | `11.13.0` |
| Shell used | Windows PowerShell `5.1.26100.8655`; command-under-test executed through `cmd.exe /d /c` |

## System-Scope Installation

| Field | Value |
| --- | --- |
| Installation command used | `npm.cmd install --global --prefix .tmp\fr20-system-scope-prefix --cache .tmp\fr20-system-scope-cache .\md2pdf-0.1.2.tgz --no-audit --no-fund` |
| Elevated privilege mechanism | `n/a` - valid simulation; no host-wide administrator install was performed |
| Installation target path | `.tmp\fr20-system-scope-prefix` |
| Installed binary path invoked | Invoked by name as `md2pdf` after adding `.tmp\fr20-system-scope-prefix` and `.tmp\fr20-system-scope-prefix\node_modules\.bin` to `PATH`; `where md2pdf` resolved `.tmp\fr20-system-scope-prefix\md2pdf` and `.tmp\fr20-system-scope-prefix\md2pdf.cmd` |

## Account Coverage

Use a real secondary user account whenever possible. A simulation is acceptable
only when it documents why a real secondary account was unavailable and proves
the same observable contract: the system-scope binary is on the invoked account's
PATH and `md2pdf --help` runs from that account context.

| Field | Value |
| --- | --- |
| Primary/installing account | `Gabriel-PC\gabri` |
| Secondary account tested | `n/a` |
| Simulation used instead of secondary account | `yes` |
| Simulation justification, if used | A real secondary Windows account and host-wide elevated install were not used because this evidence run must not mutate machine-level npm state or create/switch user accounts. The simulation proves the same Stream A observable CLI contract: the package is installed into a global-style npm prefix, that prefix is placed on `PATH`, `where md2pdf` resolves the command by name, and `md2pdf --help` exits `0` from that account context. It does not prove host-wide ACL propagation to every account. |
| Command used to switch or simulate account | `cmd.exe /d /c "set PATH=%CD%\.tmp\fr20-system-scope-prefix;%CD%\.tmp\fr20-system-scope-prefix\node_modules\.bin;%PATH%&& md2pdf --help"` |

## Command Under Test

```bash
md2pdf --help
```

Expected command shape:

```bash
md2pdf --help
```

If the binary is invoked by absolute path, record that path in "Installed binary
path invoked" and show the exact command here.

## `md2pdf --help` Output

```text
md2pdf [OPTIONS] ENTRY [ENTRY ...]

ENTRY                     a Markdown file or a directory of Markdown files
-o, --output PATH         output path for a single-file conversion
    --output-dir DIR      write every output PDF into DIR
-f, --force-overwrite     overwrite existing output PDFs without prompting
-h, --help                list options with one-line descriptions
```

## Expected Result

- The command is invocable from the tested account context.
- The command exits with status `0`.
- The help output lists the supported md2pdf CLI options.
- No TeX or LaTeX installation is required for command availability.

## Observed Result

| Field | Value |
| --- | --- |
| Command exit status | `0` |
| Was `md2pdf` invocable from the tested account? | `yes` |
| Did help output render successfully? | `yes` |
| Was TeX/LaTeX required? | `no` |
| Does this satisfy global FR-20 release evidence? | `no` |
| Deviations from expected result | Valid simulation only: this run proves command-name resolution and help execution from a global-style npm prefix on `PATH`, but not a real elevated system-scope install visible to a separate Windows account. |

## Reviewer Notes

```text
Commands executed:

git status --short
OBSERVED: working tree was already dirty before this evidence update. The
tarball below was generated from the local working tree at commit
b58c45775b5e25926d7567a230034576949bd603, not from a clean checkout.

npm.cmd run build
PASS

npm.cmd pack --json
PASS
filename: md2pdf-0.1.2.tgz
shasum: cc11a64ec297c708b2178727bd372f753fabee33
integrity: sha512-KUOkmzNX9/0yaqlkpGBFWwu/WqoWHizE4Fe1xG43cuf8JQfnGmBFaA+s3uOvQRIr3cQraFXhNlqJdO9Kk6bGdw==

npm.cmd install --global --prefix .tmp\fr20-system-scope-prefix --cache .tmp\fr20-system-scope-cache .\md2pdf-0.1.2.tgz --no-audit --no-fund
PASS
Final Point 5 replay after README/test updates: PASS, changed 123 packages.

cmd.exe /d /c "set PATH=%CD%\.tmp\fr20-system-scope-prefix;%CD%\.tmp\fr20-system-scope-prefix\node_modules\.bin;%PATH%&& where md2pdf"
PASS
C:\Users\gabri\Documents\IPSA\B2\Stage CognitiveSand\md2pdf\md2pdf\.tmp\fr20-system-scope-prefix\md2pdf
C:\Users\gabri\Documents\IPSA\B2\Stage CognitiveSand\md2pdf\md2pdf\.tmp\fr20-system-scope-prefix\md2pdf.cmd

cmd.exe /d /c "set PATH=%CD%\.tmp\fr20-system-scope-prefix;%CD%\.tmp\fr20-system-scope-prefix\node_modules\.bin;%PATH%&& md2pdf --help"
PASS

This is accepted as Stream A strict evidence for CLI invocability by name.
Global release evidence still needs Stream B browser/rendering closure.
```
